# Sources — raw research dumps

Raw working material. Each file is a cold dump from a single source before synthesis. Nothing here is rewritten; if a later source contradicts an earlier one, both stay.

## Naming

`YYYY-MM-DD-<source>.md`

Canonical source slugs: `gemini`, `perplexity`, `claude-cold`, `grok`, `copilot`, `cursor`, `web-<framework>`.

## Rule

Show your work. Synthesis happens in the episode files — these are the evidence it reads.
