You are a research subagent for Episode 2 of a living research thread on agent-architecture routing primitives. Your routing role is THE TRENCHES READ: where other models read official docs, you read GitHub issues, Reddit threads, Discord posts, recent commit logs, community forks, undocumented behaviour, and rollback stories. Surface what teams actually hit when they tried to build this — not what the README says.

CLAIM UNDER TEST:
"A working task-feature classifier is buildable today by a solo founder — but the hard part is the feature engineering, not the model. The framework primitives are ready; the routing data is not."

EPISODE 2 QUESTION:
Could a solo founder build a working task-feature classifier today — and if so, what does v0 cost?

PRIOR EPISODE CONTEXT:
Episode 1's synthesis named six candidate routing primitives:
- MasRouter (multi-agent router, ACL 2025)
- TRouter / Arch-Router / MoMA (academic routers)
- RouteLLM (LMSYS, query→model selection)
- ChatGPT's route(task, state, telemetry) → graph
- Copilot's route(task) → {execution_unit, context_projection, budget, stop_policy}
- Grok's lightweight task-feature classifier
None has won. Episode 2 asks whether a v0 is buildable today.

ANSWER ALL SIX SUB-QUESTIONS IN DEPTH. Cite GitHub issue numbers, commit SHAs, Reddit thread titles, Discord screenshots-by-quote, X/Twitter posts where you can. Speculation is fine if flagged. Length matters more than brevity here — this is a single-source dump that will be cross-referenced with three other models' outputs.

1. PREDICTIVE FEATURES — Which task features survive contact with production traffic? Which look predictive in benchmarks but break in real workloads? GitHub issues, community forum reports specifically.

2. TRAINING DATA — What labelled data have teams actually scraped together? Real cost of getting good data? What synthetic-data approaches have failed (and how)?

3. CLASSIFIER SHAPE — What classifier shapes have teams shipped and ripped out? Where does each fail in production? (LLM-as-judge, embedding+kNN, fine-tuned small model, regex/keyword, hybrid, etc.)

4. MEASUREMENT LOOP — What measurement traps have teams fallen into? What did they think proved their classifier worked but didn't? Goodhart's law cases, leakage, distribution drift discovery stories.

5. FAILURE MODES AND FALLBACKS — What rollback stories live in GitHub issues? What confidence-threshold patterns have teams converged on? When does the router get yanked entirely?

6. COST OF V0 — Real time/cost (engineer-weeks, dollars, tokens) for teams that built routing v0s. Where do they regret the investment?

SPECIFIC FRAMEWORKS / TOOLS TO COVER (TRENCHES, NOT OFFICIAL DOCS):
- RouteLLM (LMSYS) — issues, forks, real deployment stories. Anyone actually shipped it? What broke?
- MasRouter (ACL 2025) — implementation forks, "did anyone reproduce this?" threads
- LangGraph routing patterns — Cookbook examples, edge-case bug reports, Discord complaints
- OpenAI Agents SDK handoff filters — issues and discussions
- DSPy — production case studies, complaints, Omar Khattab's threads, signature drift reports
- Claude Agent SDK subagents — recent issues (skill leakage, MCP leakage, unbounded execution per Episode 1's Cursor dump showing these patterns)
- Recent arXiv papers (2025–2026) that have NOT been replicated in production — flag the gap explicitly

EMPHASIS: I want what a docs-focused model would miss.
- Newer patterns surfacing in late-2025 / early-2026 commits
- Undocumented behaviour (e.g., handoff filter ordering quirks, subagent context-bleed patterns)
- Community forks doing what mainline refuses
- Recent commits that hint at upcoming pivots
- Real rollback stories with quotes if possible

Format your response as a long, structured trenches report. Use H2 per sub-question. Within each sub-question, cite specific projects/issues/threads. End with a "what I couldn't find" section flagging gaps in your search — this matters for the cross-reference.

Don't write any files. Print everything to stdout.
